<%= f.label :title, "Give a title to your pitch *" %>
<%= f.text_field :title, placeholder: "This is a title", value: pitch.title %>
<%= errors_for(pitch, :title) %>

<%= f.label :why_me, "A short travel bio about yourself *" %>
<%= f.text_area :why_me, class: 'medium-textarea', placeholder: "type here…" %>
<%= errors_for(pitch, :why_me) %>

<%= f.label :summary, "Pitch summary *" %>
<%= f.text_area :summary, class: 'medium-textarea', placeholder: "type here…" %>
<%= errors_for(pitch, :summary) %>

<% for i in 1..3 %>
  <%= f.file_field "pictures[pic_#{i}]" %>
<% end %>
<%= errors_for(pitch, :pictures) %>

<%= f.submit submit_text, class: "button radius" %>
<%= link_to "Cancel", itinerary_path, class: "small secondary button radius", style: "margin-left:20px" %>
