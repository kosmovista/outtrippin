<nav class="top-bar">
  <ul class="title-area">
    <li class="name">
      <%= link_to root_url do %>
        <%= image_tag (defined?(@white_topbar).nil? ? 'logo-white.png' : 'OutTrippin-logo.png'), style: "margin-top:21px; height:35px;" %>
      <% end %>
      <small class="<%= (defined?(@white_topbar).nil? ? 'white' : 'primary') %>"><em>BETA</em></small>
    </li>
    <li class="toggle-topbar menu-icon"><a href=""><span></span></a></li>
  </ul>

  <section class="top-bar-section">
    <%= render 'layouts/parts/topbar/right_menu' %>
  </section>
</nav>