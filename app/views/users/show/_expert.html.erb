<div class="expert-cover">
   <%= image_tag @user.avatar.url(:thumb), class: "avatar top" %>
  <div class="row">
    <div class="large-12 columns">
      <h1 class="white edit-name-inline">
        <span>
          <%= @user.name %>
          <%= link_to '', '#' %>
        </span>
      </h1>
    </div>
  </div>
   <hr style="border:2px solid #fff;width:60px;margin:0 auto 10px auto" />
   <p class="white edit-bio-inline"><span><%= @user.expert_info[:bio].blank? ? "(click to add bio)" : @user.expert_info[:bio] %><%= link_to '', '#' %></span></p>
</div>

<div class="row top">
  <div class="large-4 columns">
    <p class="geo hometown edit-hometown-inline"><span><%= @user.expert_info[:hometown].blank? ? "(click to set your hometown)" : @user.expert_info[:hometown] %><%= link_to '', '#' %></span></p>
    <p class="geo location edit-location-inline"><span><%= @user.expert_info[:location].blank? ? "(click to set your location)" : @user.expert_info[:location] %><%= link_to '', '#' %></span></p>
    <br />
    <br />
    <%= link_to @user.website, "http://joaovalente.com", class: "www social", target: "_blank" unless @user.website.blank? %>
    <%= link_to @user.facebook, "http://#{@user.facebook}", class: "facebook social", target: "_blank" unless @user.facebook.blank? %>
    <%= link_to @user.twitter, "http://twitter.com/#{@user.twitter}", class: "twitter social", target: "_blank" unless @user.twitter.blank? %>
    <%= link_to @user.instagram, "http://instagram.com/#{@user.instagram}", class: "instagram social", target: "_blank" unless @user.instagram.blank?%>
    <br />
    <br />
  </div>

  <div class="large-8 columns">
    <% unless @user.expert_info[:style].blank? %>
    <ul class="inline-list skill-list">
      <% @user.expert_info[:style].each do |s| %>
        <li><span class="label light round"><%= s %></span></li>
      <% end %>
    </ul>
    <% end %>

    <h2 class="subheader">My Story</h2>
    <p class="edit-story-inline"><span><%= @user.expert_info[:story].blank? ? "(click to add a story)" : @user.expert_info[:story] %><%= link_to '', '#' %></span></p>

    <h2 class="subheader">My Best Travel Hack</h2>
    <p class="edit-travel_hack-inline"><span><%= @user.expert_info[:travel_hack].blank? ? "(click to add travel hacks)" : @user.expert_info[:travel_hack] %><%= link_to '', '#' %></span></p>

    <h2 class="subheader">My Places</h2>
    <ul class="inline-list skill-list">
      <% unless @user.expert_info[:countries].blank? %>
        <% @user.expert_info[:countries].each do |c| %>
          <li><span class="label light round"><%= c %></span></li>
          <% end %>
      <% end %>

      <% unless @user.expert_info[:cities].blank? %>
        <% @user.expert_info[:cities].each do |c| %>
          <li><span class="label light round"><%= c %></span></li>
        <% end %>
      <% end %>
    </ul>

    <%= text_field_tag :lorem, nil, class: 'location-autocomplete' %>

  </div>
</div>

<%= content_for :head do %>
  <%= stylesheet_link_tag "experts", media: "all", "data-turbolinks-track" => true %>
  <script src="http://maps.googleapis.com/maps/api/js?libraries=places&amp;sensor=false"></script>
<% end %>